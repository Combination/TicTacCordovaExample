const React = require('react');
const ReactDOM = require('react-dom');
const Application = require('./application.react');

ReactDOM.render(
    <Application />,
    document.getElementById('js-app')
);